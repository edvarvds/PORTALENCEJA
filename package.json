{
  "name": "portal-inceja",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install && npm run build",
    "heroku-postbuild": "npm install typescript && cd frontend && npm install && npm run build"
  }
} 